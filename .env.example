# =====================================================================
# Lexato Chrome Extension - Variáveis de Ambiente
# =====================================================================
# Copie este arquivo para .env.local e preencha com suas credenciais.
# NUNCA commite .env.local ou .env.production — ambos estão no .gitignore
# =====================================================================

# --- Supabase ---
# URL do seu projeto Supabase (ex: https://XXXX.supabase.co ou domínio customizado)
# Encontre em: Supabase > Settings > API > Project URL
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co

# Chave pública anônima do Supabase (anon key — segura para expor no cliente)
# Encontre em: Supabase > Settings > API > Project API keys > anon/public
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# --- Google OAuth2 ---
# client_id para autenticação nativa via chrome.identity
# Como obter:
#   1. Acesse https://console.cloud.google.com/
#   2. Crie credenciais OAuth 2.0 do tipo "Extensão Chrome"
#   3. Cole o client_id aqui
# Documentação: https://developer.chrome.com/docs/extensions/mv3/tut_oauth/
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# --- Backend Lexato ---
# URL base da API REST (omita a barra final)
# Exemplos:
#   dev:     http://127.0.0.1:3000
#   staging: https://api-staging.lexato.com.br
#   prod:    https://api.lexato.com.br
VITE_API_BASE_URL=http://127.0.0.1:3000

# URL do WebSocket para notificações em tempo real
# Exemplos:
#   dev:     ws://127.0.0.1:3001
#   staging: wss://ws-staging.lexato.com.br
#   prod:    wss://ws.lexato.com.br
VITE_WS_URL=ws://127.0.0.1:3001

# URL do frontend principal (usada em links de redirecionamento pós-login)
VITE_FRONTEND_URL=http://127.0.0.1:3000

# --- Ambiente ---
# Controla qual conjunto de configurações usar: dev | staging | prod
# Se omitido, será inferido automaticamente pelo modo do Vite
VITE_LEXATO_ENV=dev

# --- Sentry (Monitoramento de Erros - Opcional) ---
# DSN do projeto Sentry para rastreamento de erros em produção
# Deixe em branco para desabilitar o Sentry em desenvolvimento
# Como obter: https://docs.sentry.io/product/sentry-basics/dsn-explainer/
VITE_SENTRY_DSN=

# --- Debug ---
# Define true para habilitar logs verbosos no console
VITE_DEBUG=true

# --- DevTools ---
# Habilita verificação de DevTools aberto durante a captura
VITE_DEVTOOLS_CHECK_ENABLED=false
